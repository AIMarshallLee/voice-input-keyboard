# VoType 1.0 build 127 — merged-code gate evidence

| Field | Evidence |
| --- | --- |
| Source commit | `e8bfb57296ff4aab98b49b71adfe7b38fd744119` on `main` |
| Actions run | [Build IPA #127](https://github.com/AIMarshallLee/voice-input-keyboard/actions/runs/32870175097), run ID `32870175097` |
| Unit tests | 73 executed, 0 failures |
| UI tests | 2 unique tests, 0 failures |
| Pressure case | `testTwentySequentialSessionRoundTripsLeaveNoStaleState` passed in 1.842 s |
| Configuration gate | Generated distribution Info.plist validator passed |
| Device build | Unsigned generic iphoneos Release build succeeded |
| IPA artifact | `VoType-IPA`, artifact ID `9572186636`, 1,298,949 bytes |
| Artifact ZIP SHA-256 | `2bfa727b9661ab14b24a1dc591c7c5e2e1f711d90e78205ed7143bec64065835` |
| Retention | GitHub Actions artifact retention: 30 days |
| Distribution state | Not signed for App Store and not uploaded; this is a merged-code build gate |

## Known build warnings

- Swift actor-isolation warnings remain in `BackgroundDictationManager.swift`
  and `DictationView.swift` under Swift 5.9.
- `UIButton.contentEdgeInsets` is deprecated when using button configuration.
- App Intents metadata extraction is skipped because the app has no App Intents dependency.

These warnings did not fail the Release build, but they are recorded as future
Swift 6/UI cleanup and do not replace final-device verification.

## Promotion rule

Promote only a later App Store-signed build produced from a commit containing
this gate plus the commercial-candidate documentation. Record its signature,
artifact, App Store processing and internal-testing evidence in a separate
versioned file. No CI result proves microphone, PiP, third-party insertion,
energy, memory or App Review behavior on a physical device.
