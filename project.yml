name: VoiceInputKeyboard
options:
  bundleIdPrefix: com.voiceinput
  deploymentTarget:
    iOS: "16.0"
  createIntermediateGroups: true

targets:
  # 主 App:引导安装、设置、隐私说明
  VoiceInputApp:
    type: application
    platform: iOS
    sources:
      - path: VoiceInputApp
        excludes:
          - "Info.plist"
          - "VoiceInputApp.entitlements"
    info:
      path: VoiceInputApp/Info.plist
      properties:
        CFBundleDisplayName: 语音输入键盘
        CFBundleShortVersionString: "1.0"
        CFBundleVersion: "1"
        LSRequiresIPhoneOS: true
        NSMicrophoneUsageDescription: 需要使用麦克风来进行语音转文字输入
        NSSpeechRecognitionUsageDescription: 需要语音识别权限将你说的话转换成文字
        UILaunchScreen:
          UIColorName: ""
        UISupportedInterfaceOrientations:
          - UIInterfaceOrientationPortrait
          - UIInterfaceOrientationLandscapeLeft
          - UIInterfaceOrientationLandscapeRight
        UISupportedInterfaceOrientations~ipad:
          - UIInterfaceOrientationPortrait
          - UIInterfaceOrientationPortraitUpsideDown
          - UIInterfaceOrientationLandscapeLeft
          - UIInterfaceOrientationLandscapeRight
    settings:
      base:
        PRODUCT_BUNDLE_IDENTIFIER: com.voiceinput.app
        MARKETING_VERSION: "1.0"
        CURRENT_PROJECT_VERSION: "1"
        SWIFT_VERSION: "5.9"
        IPHONEOS_DEPLOYMENT_TARGET: "16.0"
        TARGETED_DEVICE_FAMILY: "1,2"
        GENERATE_INFOPLIST_FILE: NO
        CODE_SIGN_ENTITLEMENTS: VoiceInputApp/VoiceInputApp.entitlements
    dependencies:
      - target: KeyboardExtension

  # 键盘扩展:语音输入核心功能
  KeyboardExtension:
    type: app-extension
    platform: iOS
    sources:
      - path: KeyboardExtension
        excludes:
          - "Info.plist"
          - "KeyboardExtension.entitlements"
    info:
      path: KeyboardExtension/Info.plist
      properties:
        CFBundleDisplayName: 语音输入
        CFBundleShortVersionString: "1.0"
        CFBundleVersion: "1"
        NSMicrophoneUsageDescription: 需要使用麦克风来进行语音转文字输入
        NSSpeechRecognitionUsageDescription: 需要语音识别权限将你说的话转换成文字
        NSExtension:
          NSExtensionAttributes:
            IsASCIICapable: false
            PrefersRightToLeftLayout: false
            PrimaryLanguage: zh-Hans
            RequestsOpenAccess: true
          NSExtensionPointIdentifier: com.apple.keyboard-service
          NSExtensionPrincipalClass: $(PRODUCT_MODULE_NAME).KeyboardViewController
    settings:
      base:
        PRODUCT_BUNDLE_IDENTIFIER: com.voiceinput.app.keyboard
        SWIFT_VERSION: "5.9"
        IPHONEOS_DEPLOYMENT_TARGET: "16.0"
        GENERATE_INFOPLIST_FILE: NO
        CODE_SIGN_ENTITLEMENTS: KeyboardExtension/KeyboardExtension.entitlements
